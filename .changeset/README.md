# Changesets

This workspace uses Changesets for versioning and publishing packages.

- Add a changeset: `pnpm changeset`
- Version packages: `pnpm version-packages`
- Publish (after build): `pnpm release`
